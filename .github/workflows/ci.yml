# CI disabled — deployment is handled via manual zip upload to Elastic Beanstalk.
# See README for deploy instructions.
name: CI
on:
  push:
    branches: [never-run]
jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - run: echo "CI disabled"
